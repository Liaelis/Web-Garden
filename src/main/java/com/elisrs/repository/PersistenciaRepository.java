package com.elisrs.repository;

import org.springframework.data.jpa.repository.JpaRepository;

import com.elisrs.enty.Persistencia;

public interface PersistenciaRepository extends JpaRepository<Persistencia, Integer>{

}
